'use strict'

// Import Expect
const expect = require('chai').expect;

// CLI Help Menu Test
describe('Test CLI Help Menu Output', () => {

    // Help Menu without input
    describe('Help Menu without input', () => {

        // It should show the help menu if no CLI input is given
        it('Should show the help menu if no CLI input is given', () => {
            // Expectation

        });
    });

    // Help Menu with -h input
    describe('Help Menu with -h input', () => {

    });
});